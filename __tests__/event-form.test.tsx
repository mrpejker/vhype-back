// import React from 'react';
// import { render, screen, fireEvent } from '@testing-library/react';
// import EventForm from '../features/event-form/event-form';

describe('EventForm', () => {
  test('renders the event form with initial values', () => {
    //     // eslint-disable-next-line @typescript-eslint/no-empty-function
    //     render(<EventForm submitForm={() => {}} />);
    //     // Assert that the form fields and buttons are rendered correctly
    //     expect(screen.getByLabelText('Event title')).toBeInTheDocument();
    //     expect(screen.getByLabelText('Event description')).toBeInTheDocument();
    //     expect(screen.getByRole('button', { name: 'Create New Event' })).toBeInTheDocument();
  });
  //   test('updates event title when input value changes', () => {
  //     render(<EventForm submitForm={() => {}} />);
  //     const eventTitleInput = screen.getByLabelText('Event title');
  //     fireEvent.change(eventTitleInput, { target: { value: 'New Event Title' } });
  //     // Assert that the event title has been updated
  //     expect(eventTitleInput.value).toBe('New Event Title');
  //   });
  //   // Write more tests for other form fields and functionalities
});
